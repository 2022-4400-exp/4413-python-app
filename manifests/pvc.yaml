apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: python-app-data-pvc
spec:
  resources:
    requests:
      storage: 100M
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
